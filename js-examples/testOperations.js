var number = 1;

number += 3; /* number = number + 3 */;
console.log(number);

// subsstruction
number = number - 2;
number -= 2;

// multiplication
number = number * 2;
number *= 2;

// Div
number = number / 2;
number /= 2;

// Exponentation
namer = number ** 2
console.log(3 ** 2);
