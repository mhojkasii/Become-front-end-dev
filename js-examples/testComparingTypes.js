var str = "1";
var number = 1;

// compare values
if (str == number) {
    console.log("we are equal 1");
}

// compare value and TYPE
if (str === number) 
{
    console.log("we are equal 2");
}